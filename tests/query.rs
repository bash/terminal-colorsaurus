const FG_EXE: &str = env!("CARGO_BIN_EXE_fg");

// Todo Tests:
// * Terminal does not respond at all
// * Terminal only supports OSC c
// * Terminal supports querying (but is not known to do so)
// * Terminal has known support
// * Terminal has known non-support
// * All stdio is redirected
// * stdout, stderr is redirected and stdin is read-only.
