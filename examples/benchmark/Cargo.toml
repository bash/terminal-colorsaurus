[package]
name = "benchmark"
edition = "2021"
version = "1.0.0"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
console = "0.15.8"
indicatif = "0.17.7"
terminal-colorsaurus = { path = "../.." }
